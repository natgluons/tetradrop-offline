buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // CHANGE THIS LINE:
        classpath 'com.android.tools.build:gradle:8.9.1'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
